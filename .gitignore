.DS_Store
.source_files

# Bulk asset libraries — stored outside the repo, symlinked in.
# See ~/Projects/assets/ for actual files.
/sound_effects

# LoRA training inputs — datasets of image/caption pairs, stored outside tracking.
# Product artifacts (.safetensors, .ckpt) remain tracked under lora/ via LFS.
/lora_datasets/
