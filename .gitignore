# macOS
.DS_Store
AB615685-*

# LoRA training data (committed from oq-2, local copies are duplicates)
lora/

# Local build/run artifacts
.source_files/
runs/

# Commercial sound library (licensed, not redistributable) — symlinked out of repo
sound_effects
